include ../loader/loader

//- .hidden
section.container.question#getNane.hidden
    .row
        .col-12.animatible
            .get-name__caption
                h2.get-name__title
                    | Укажите имя для регистрации
                    br
                    | в системе

        .col-12.animatible
            .get-name__form
                form.form(action="" method="GET")
                    input(type="hidden" value="form-name" name="form")
                    label.form__group
                        input#name(
                            type="text"
                            name="name"
                            placeholder="Введите Ваше имя")

                    label.form__group
                        button.btn.btnNextStep.animation-line(
                            type="button"
                            data-next-step-id="#getContact"
                            disabled) к результату

        .col-12.animatible
            .get-name__loader
                h2.get-name__title
                    | Формируем предварительный
                    br
                    | результат

                +loader

        .col-12.animatible
            .get-name__check-list
                ul
                    li
                        include ../../../img/icon-check-list.svg
                        | Формируем подборку

                    li
                        include ../../../img/icon-check-list.svg
                        | Готовим чек-лист
